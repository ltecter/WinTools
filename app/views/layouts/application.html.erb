<!DOCTYPE html>
<html>
<head>
  <title>WinTools</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>  
<body>
  <div id="wrapper">
    <header>
      <h1>WinTools</h1>
      <h3>Reasonably Priced Automotive Tools</h3>
    </header>  
    <nav>
      <%= link_to "Home", root_path %> 
      <% @categories.each do |category| %>
        | <%= link_to category.name, products_by_category_path(category.id) %>
      <% end %>
    </nav>
    <%= render :partial => "products/cart", :locals => { :cart => @items, :cost => @cost, :total => @total, :num_items => @qty } %>
    <div id="search">
      <%= render :partial => "/search/search" %>
    </div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    
    <%= yield %>
    <footer>
      <h5>Copyright &copy; WinTools Leonard Tecter 2013</h5>
    </footer>
  </div>  
</body>
</html>
